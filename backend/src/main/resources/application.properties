# Server port (Render requirement)
server.port=${PORT:8080}

# Supabase Requirements
spring.datasource.url=${SUPABASE_DB_URL:jdbc:postgresql://aws-0-ap-south-1.pooler.supabase.com:6543/postgres}
spring.datasource.username=${SUPABASE_DB_USERNAME:postgres.ccnxzkfofjubijnarxmf}
spring.datasource.password=${SUPABASE_DB_PASSWORD:}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

spring.security.oauth2.resourceserver.jwt.issuer-uri=${SUPABASE_JWT_ISSUER:https://ccnxzkfofjubijnarxmf.supabase.co/auth/v1}
app.cors.allowed-origins=${APP_CORS_ALLOWED_ORIGINS:https://crypto-portfolio-tracker-fvi0.onrender.com}

supabase.url=${SUPABASE_URL:}
supabase.key=${SUPABASE_SERVICE_KEY:}


spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.idle-timeout=30000
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.max-lifetime=1800000

# JWT
jwt.secret=${JWT_SECRET:change-me-to-a-very-long-secret-key-for-prod-1234567890}
jwt.expirationMs=${JWT_EXPIRATION_MS:900000}
jwt.refreshExpirationMs=${JWT_REFRESH_EXPIRATION_MS:604800000}

# Mail server configuration
spring.mail.host=${SMTP_HOST:localhost}
spring.mail.port=${SMTP_PORT:1025}
spring.mail.username=${GMAIL_USERNAME:}
spring.mail.password=${GMAIL_PASSWORD:}
spring.mail.properties.mail.smtp.auth=${SMTP_AUTH:false}
spring.mail.properties.mail.smtp.starttls.enable=${SMTP_STARTTLS:false}


# Gemini API Key
gemini.api.key=${GEMINI_API_KEY:}
gemini.api.url=${GEMINI_API_URL:https://generativelanguage.googleapis.com}

app.crypto.secret=${APP_CRYPTO_SECRET:0123456789abcdef0123456789abcdef}


